### CleanerCSV datafile cleaner

A more detialed explanation is provided in the apps GUI.

Launch app.py with no parameters for GUI.

Launch app.py <specs_file_name.JSON> to process a file without loading the GUI.

### Required packages:
pandas
ydata_profiling
PyQt6 for GUI